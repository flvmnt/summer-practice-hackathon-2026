# ShowUp2Move — Δ Audit + Historical Sweep (2026-05-09 13:23)

**T0:** 2026-05-09 12:47 (last security audit)
**Now:** ~13:25 local · **2.5 h until demo**
**Mode:** Delta + Historical Sweep, both fired in parallel
**Aim:** the full **16,600 p** rubric

---

## TL;DR — sprint headline

**Score TODAY: ~8,900 / 16,600 (53.6%)** — up **+5,680 p** in 36 min since the security audit. Velocity peaked at this window (1.58 commits/hour overall, with a 12:30-13:20 cluster).

**Single highest-leverage move with 2.5 h left:** **Groq text wrapper + bio→sport extraction.** The AI bucket is at 0/2,800 p — wiring text extraction unlocks ~1,900 p in 30 min. Photo upload chains into AI vision and adds another ~700 p in 45 min.

**Demo-impact UI gap:** the signature animations (RatchetRow, TypeOn, VoteCard) are written as components but **uncommitted in the dirty worktree**. Committing them and wiring them in adds ~40% of the interaction-polish delta and is what judges visually see first.

| Lane | Status |
|---|---|
| Score (committed) | **8,900 / 16,600** (53.6%) |
| Δ since 12:47 | **+5,680 p** in 36 min |
| Commits | 37 total · 1.58/hr · 0 regressions |
| Avg commit grade | **B** (15 A, 17 B, 5 C) |
| Test-on-new-code | **8%** — major debt |
| Schema | **81%** (17/21 tables) · aiCache too minimal |
| Design tokens | **10/10** — direction-B canon (sodium #FF5C2A + turf #1E6E48) matches perfectly |
| Design layout | 7.6/10 |
| Design interaction polish | **5.9/10** — signature animations uncommitted |
| **Design composite** | **7.1/10** |
| Copy/i18n | **0 RO violations, 0 EN violations, html-lang FIXED, parity ✓** |
| Auth/security (post my 5 fixes) | **B → A−** |

---

## 1. Δ commits since T0 (12:47)

7 teammate commits + 5 of mine + 1 fix smoke:

| SHA | Subject | Lane |
|---|---|---|
| `b3c6645` | fix(auth): bcrypt cost 12 + dummies regenerated | Auth (mine) |
| `027db43` | feat(headers): CSP + HSTS | A05 (mine) |
| `5a0afeb` | feat(auth): requireUser + requireAdmin helpers | A01 (mine) |
| `4ae8f16` | fix(env): require DATABASE_URL/SESSION_SECRET/PUBLIC_BASE_URL in prod | A02 (mine) |
| `69261f2` | feat: robots.txt | A05 (mine) |
| `c51af96` | fix: harden production chat smoke | Chat (teammate) |
| `6eaf47f` | feat: event creation + event chat | **Communication 1,000 p** |
| `c611e17` | feat: venue candidates + voting | **Event coord 1,000+ p** |
| `f63b3a2` | feat: direction-B brand tokens | Design polish |
| `2f4df2c` | feat: calendar export | **Bonus 300 p** |
| `a7a560f` | docs: direction-B brand spec update | Spec |
| `b7abe93` | db: ai_cache table | AI prep |
| `56246f5` | feat: event weather forecast | **Bonus 300 p** |

---

## 2. Stagnant rubric rows (highest-points-per-hour to attack)

| Row | Cap | Today | Spec ref | Why stagnant |
|---|---:|---:|---|---|
| AI bio extraction | 500 | 0 | `05-ai-features.md:41-79` | Groq client not wired |
| AI photo→sport (vision) | 500 | 0 | `05-ai-features.md:81-114` | Groq vision not wired + no upload pipe |
| AI compatibility scoring | 300 | 0 | `05-ai-features.md:116-143` | depends on bio extraction |
| Smart teammate recs | 300 | 0 | `05-ai-features.md:145-161` | depends on AI |
| Profile photo upload | 200 | 0 | `03-server-actions-and-routes.md:144-149` | R2 wiring not started; **R2 vars now set on Railway** ✓ |
| Match confirmation workflow | 300 | 0 | `03-server-actions-and-routes.md:64` | one server action away |
| Notifications/SSE | 300 | 0 | `03-server-actions-and-routes.md:178-193` | no notif table or SSE route |

**Stagnant subtotal: 2,400 p uncaptured, all closable in ≤2 h with focused execution.**

---

## 3. Design canon delta — what judges will see

> Full report: `_partials-history-2026-05-09-1323/02-design-canon-delta.md`

Aggregate adherence: **7.1/10**. Token fidelity is perfect. Interaction polish is the drag.

**Top 3 fixes (≤25 min combined, +40% of interaction delta):**

1. **Commit + wire `RatchetRow`** to `TodayPromptCard` searching state — 10 min. Canon shows "8 nearby → 6 available → 4 matches" count-up animation; current `/today` skips this entirely. **Highest judge-impact** because it's the core demo loop's signature moment.
2. **Restore button shadows + active-scale** in `globals.css` `.btn-s2m` — 8 min. Login/signup buttons currently look flat vs canon.
3. **Wire `AIMark` into `TodayPromptCard`** — 5 min. The AIMark component exists; just import + render in the searching state's accent-tint box. Brands AI involvement visually.

(All three are dirty-worktree-resident or trivial CSS — coordinate with the worktree owner before commit.)

**Other dirty-worktree components ready to commit and wire:**
- `TypeOn` — character reveal (VenueRow, CaptainRevealScreen)
- `VoteCard` — interactive venue vote panel
- `WizardMobileHeader` + `WizardStickyActionBar` — closes the mobile-first responsive UI gap on onboarding
- `CaptainBriefPanel`, `FormationTimeline` — innovation-row distinctive surfaces (cited in agent-1's earlier `13-scoring-coverage.md` audit as worth +200-400 p innovation)

---

## 4. Schema + AI infrastructure delta

> Full report: `_partials-2026-05-09-1323/03-schema-tokens.md`

- **17 of 21 spec'd tables present (81%)**. Missing: `threadReads`, `notifications`, `stravaAccounts`, `pushSubscriptions`, `achievements`. Notifications + threadReads matter for the rubric; the others are stretch.
- **`ai_cache` table is too minimal** — committed schema only has `(input_hash, output_json, created_at)`. Spec 02 calls for `(key, kind, model, input_hash, output, expires_at, created_at)`. Without `kind` + `expires_at`, demo namespacing and TTL don't work. **Fix in next migration.**
- **Migrations 0000-0007 forward-only, no DROP/unsafe ALTERs**. Safe to apply to Railway.
- **R2 env vars now set on Railway** ✓ — photo upload row is unblocked the moment the upload handler is written.

---

## 5. Copy/i18n/a11y status

> Full report: `_partials-2026-05-09-1323/02-copy-i18n-a11y.md`

- **0 RO diacritic violations** in committed `messages/ro/common.json` ✓
- **0 EN brand-voice violations** ✓ (all `Timișoara` instances correct)
- **`pnpm lint:i18n` passes** ✓
- **0 hard-coded JSX strings** in committed code
- **`<html lang>` is now dynamic** (uses `await getLocale()`) — the long-running regression is **fully resolved** ✓
- A11y micro-debt on auth forms: **missing `htmlFor` and `aria-busy` on auth components** — non-blocking, 5-min fix

**Composite copy/i18n grade: B+**

---

## 6. Risk register diff

| Risk | T0 (12:47) | Now |
|---|---|---|
| Bcrypt cost 10 | OPEN | ✅ **FIXED** (`b3c6645`) |
| No CSP/HSTS | OPEN | ✅ **FIXED** (`027db43`) |
| `requireUser()` helper missing | OPEN | ✅ **FIXED** (`5a0afeb`) — adoption pending |
| Env validation lenient | OPEN | ✅ **FIXED** (`4ae8f16`) |
| No robots.txt | OPEN | ✅ **FIXED** (`69261f2`) |
| Migration not applied to Railway | OPEN | 🚧 **R2 vars now in place** — push will run all 8 migrations |
| AI bucket 0/2,800 | OPEN | OPEN — **biggest scoring lever left** |
| Photo upload 0/200 | OPEN | OPEN — R2 unblocked, just needs handler |
| Notifications row 0/300 | OPEN | OPEN |
| Test-on-new-code 8% | NEW | OPEN — accept for hackathon, hot-path tests post-demo |
| aiCache table minimal | NEW | OPEN — needs `kind` + `expires_at` columns |
| 30+ UI components uncommitted | NEW | OPEN — major polish trapped in worktree |

---

## 7. The 2.5-hour critical path (ranked by points/hour)

Time budget: ~150 min. Each step assumes prior steps shipped. Cap each at the time shown.

| # | Step | Time | Pts unlocked | Pts/hour |
|---|---|---:|---:|---:|
| 1 | **Commit dirty UI batch** (ratchet, type-on, vote-card, sticky bars, AIMark wiring, button shadows) | 15 min | +200-400 (UX/Innovation) | ~1,200 |
| 2 | **Groq text client + bio→sport extraction + cache** | 30 min | +800 (AI bio) +500 wired into compat scoring | **~2,600** |
| 3 | **Photo upload (R2 + sharp re-encode + UI step)** | 30 min | +200 (photo) +500 (photo→sport vision) | **~1,400** |
| 4 | **Match confirmation workflow** | 20 min | +300 | ~900 |
| 5 | **Smart teammate recs UI in invite drawer** | 25 min | +300 | ~720 |
| 6 | **`/api/health` Groq probe + AIMark in /today + RatchetRow live** | 15 min | +200 (innovation/insurance) | ~800 |
| 7 | **Loom recording of full flow as fallback** | 15 min | preserves all | ∞ |

**Sum:** ~150 min · **+3,000-3,500 p** · projected demo total **~12,000-12,500 p**.

---

## 8. Don't-build (cuts if any of #1-#7 slips)

- Strava OAuth (500 p, brittle, last-cut)
- Resend email reminders (150 p, in-app SSE banner already covers most credit)
- Achievements beyond First Match (150 p)
- SonarCloud / pnpm audit CI (150 p)
- threadReads + notifications tables (300 p — only if Notifications row is sacrificed)

---

## 9. Coordination notes

- **Dirty worktree owns 30+ uncommitted UI components** — per-component list in `_partials-history-2026-05-09-1323/02-design-canon-delta.md`. Coordinate before edits to avoid conflicts.
- **Railway env vars: 14/14 set** ✓ (incl. GROQ_API_KEY + 5 R2 vars).
- **Last push: HEAD `2f4df2c`.** Several commits since then haven't deployed. Next `git push` will trigger Railway redeploy with migrations 0005-0007.
- Audit dir `docs/audit/` is still untracked — `git add docs/audit/ && git commit` to preserve.

---

## 10. Footer — what to do RIGHT NOW

If decision-time → **Step 2 (Groq text wrapper)** is the highest p/h move on the board. Want me to draft the Groq client + bio extraction + cache integration now (≤25 min of focused implementation, hands-on), and have the team pick up step 3 in parallel?

— end of delta + history audit —
